# Tablet App APK 构建说明

## 当前问题和修复

### 已修复的问题
1. ✅ **Gradle 下载超时** - 配置了国内镜像
2. ✅ **React 版本不兼容** - 降级到 React 18.3.1
3. ✅ **Kotlin 版本不支持 KSP** - 升级到 Kotlin 2.0.21
4. ✅ **enableBundleCompression 不存在** - 已删除该配置
5. ✅ **TurboModule 错误** - 关闭新架构（newArchEnabled=false）

### 当前配置
- **Expo SDK**: 54.0.0
- **React**: 18.3.1
- **React Native**: 0.76.5
- **Kotlin**: 2.0.21
- **新架构**: 关闭（false）
- **Hermes**: 启用（true）

## 构建方式

### 方式 1：云端构建（推荐）✅
**优点**：
- 不需要本地安装 Android Studio
- 不需要配置 ANDROID_HOME
- 构建环境统一，成功率高
- 自动处理签名

**步骤**：
1. 运行 `FINAL-FIX-AND-BUILD.bat`
2. 等待 10-20 分钟
3. 收到邮件通知后下载 APK

**命令**：
```bash
cd tablet-app
npx eas-cli build --platform android --profile production --clear-cache
```

### 方式 2：本地构建
**缺点**：
- 需要安装 Android Studio
- 需要配置 ANDROID_HOME 环境变量
- 需要下载大量依赖（几个 GB）
- 首次构建很慢（30分钟+）

**步骤**：
1. 安装 Android Studio
2. 配置环境变量：`ANDROID_HOME=C:\Users\你的用户名\AppData\Local\Android\Sdk`
3. 运行 `npx expo run:android --variant release`

## 重要提示

### ⚠️ 不要混淆开发模式和构建模式

**开发模式**（`npm start` 或 `expo start`）：
- 启动 Metro 开发服务器
- 用 Expo Go 扫码运行
- 不会生成 APK
- 用于开发调试

**构建模式**（`eas build`）：
- 生成独立的 APK 文件
- 可以直接安装到设备
- 用于生产发布

### 🔍 如何判断是否在构建

看到这些说明你在**开发模式**：
- `Starting Metro Bundler`
- `Scan the QR code`
- `exp://192.168.x.x:8081`

看到这些说明你在**构建模式**：
- `Compressing project files`
- `Uploaded to EAS`
- `See logs: https://expo.dev/...`

## 常见错误

### 错误 1：TurboModule 找不到
**原因**：新架构不稳定
**解决**：设置 `newArchEnabled=false`

### 错误 2：enableBundleCompression 不存在
**原因**：新版本 RN 不支持
**解决**：删除 `build.gradle` 中的该行

### 错误 3：Kotlin 不支持 KSP
**原因**：Kotlin 版本太低
**解决**：升级到 Kotlin 2.0.21

## 构建配置文件

### `eas.json`
```json
{
  "build": {
    "production": {
      "android": {
        "buildType": "apk",
        "gradleCommand": ":app:assembleRelease"
      }
    }
  }
}
```

### `android/gradle.properties`
```properties
newArchEnabled=false
hermesEnabled=true
android.kotlinVersion=2.0.21
```

## 下一步

1. 运行 `FINAL-FIX-AND-BUILD.bat`
2. 等待构建完成
3. 下载 APK
4. 安装测试

如果还有问题，查看构建日志：https://expo.dev
